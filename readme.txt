1)
use command $nodemon app.js or $node app.js to run the server on port 3000
2) basic login and register is added on /vendor,/user,/admin
3) Also other routes has been added and can be accessed using /{routename}